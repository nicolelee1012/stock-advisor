# Python
.venv/
venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Streamlit secrets (never commit API keys)
.streamlit/secrets.toml

# Large, regenerable per-ticker caches (the consolidated CSVs ARE committed)
data/edgar/
data/estimates/

# Logs
data/*.log

# NOTE: we intentionally COMMIT these so the deployed dashboard + Action have data:
#   data/sp500.csv  data/model.joblib  data/edgar_facts.csv
#   data/trading.db  data/backtest_*.csv
